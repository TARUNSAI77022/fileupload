apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-deployment
  namespace: bolx
  labels:
    app: bolxwmsdev
spec:
  replicas: 3
  selector:
    matchLabels:
      app: bolxwmsdev
  template:
    metadata:
      labels:
        app: bolxwmsdev
    spec:
      imagePullSecrets:
      - name: dockerhub
      initContainers:
      - name: init-file-upload
        image: busybox
        command: ['sh', '-c', 'cp -r /workspace/* /app/wwwroot/upload']
        volumeMounts:
        - mountPath: /workspace
          name: jenkins-workspace-volume
        - mountPath: /app/wwwroot/upload
          name: file-upload-volume
      containers:
      - name: bolxwms
        image: aravindaravind/fileupload-app:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        volumeMounts:
        - mountPath: /app/wwwroot/upload
          name: file-upload-volume
      volumes:
      - name: file-upload-volume
        persistentVolumeClaim:
          claimName: file-upload-pvc
      - name: jenkins-workspace-volume
        hostPath:
          path: /var/lib/jenkins/workspace/kube/FileUpload/wwwroot/upload/
