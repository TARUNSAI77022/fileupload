apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-deployment
  namespace: bolx
  labels:
    app: bolxwmsdev
spec:
  replicas: 3
  selector:
    matchLabels:
      app: bolxwmsdev
  template:
    metadata:
      labels:
        app: bolxwmsdev
    spec:
      containers:
      - name: bolxwms
        image: aravindaravind/fileupload-app:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        volumeMounts:
        - mountPath: /app/wwwroot/upload
          name: file-upload-volume
      - name: file-upload-sidecar
        image: some-sidecar-image:latest
        imagePullPolicy: Always
        command: ['sh', '-c', 'rsync -au /mnt/upload/ /app/wwwroot/upload']  # Use rsync for efficiency
        volumeMounts:
        - mountPath: /mnt/upload
          name: file-upload-volume
      volumes:
      - name: file-upload-volume
        persistentVolumeClaim:
          claimName: file-upload-pvc
      imagePullSecrets:
      - name: dockerhub
